import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  bottomBtn: {
    position: "absolute",
    alignSelf:'center',
    bottom: 10,
    width: "100%",
    // right: 0,
    // left: 20,
  },
});

